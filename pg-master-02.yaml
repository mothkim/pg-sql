apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-master-02
  namespace: pg-sql-02
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /nfs/home/data/database/pg-sql-02/master
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-master-02
  namespace: pg-sql-02
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: manual
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-sfs-02
  namespace: pg-sql-02
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-02
  template:
    metadata:
      labels:
        app: postgres-02
    spec:
      containers:
        - name: postgres-02
          image: postgres:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: mydatabase
            - name: POSTGRES_USER
              value: myuser
            - name: POSTGRES_PASSWORD
              value: mypassword
            - name: POSTGRESQL_PGAUDIT_LOG
              value: 'read,write'
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc-master-02
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service-02
  namespace: pg-sql-02
spec:
  selector:
    app: postgres-02
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432